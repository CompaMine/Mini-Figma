# Figcore — отчёт о замерах

Машина: Windows 10, Chrome headless + ANGLE (реальный GPU). Сцена целиком влезает в кадр (fit-to-bounds), автопан 3 с. FPS упирается в интервал `requestAnimationFrame` (~237 Гц в headless; на обычном мониторе это будет 60). Поэтому рядом с FPS смотрим **CPU-время кадра** (`draw`): именно оно растёт с N.

## Цифры

| бэкенд | N | генерация | FPS | кадр rAF | draw CPU |
|---|---:|---:|---:|---:|---:|
| Wasm + WebGL2 instancing | 1 000 | 0.2 ms | 237 | 4.2 ms | 0.03 ms |
| Wasm + WebGL2 | 10 000 | 0.8 ms | 237 | 4.2 ms | 0.03 ms |
| Wasm + WebGL2 | 100 000 | 5.3 ms | 237 | 4.2 ms | 0.03 ms |
| JS-объекты + Canvas 2D | 1 000 | 1.1 ms | 237 | 4.2 ms | 0.61 ms |
| JS + Canvas 2D | 10 000 | 2.5 ms | 58 | 17 ms | 5.7 ms |
| JS + Canvas 2D | 100 000 | 21 ms | **6.3** | 158 ms | **125 ms** |

Кнопка «Замерить 3с» в UI повторяет этот протокол на вашей машине.

## Где узкие места

**Наивный JS.** На 10k уже не держит 60 FPS. На 100k главный поток ~125 ms на кадр: обход массива объектов, `fillRect` / `ellipse` по одному, нет батчинга. Hit-test кликом — тот же линейный обход в JS. Пан/зум перерисовывают всё заново.

**Wasm без смены рендерера сам по себе сцену не спасает.** Генерация 100k в Rust быстрее (~4×), hit-test и undo дешевле, но если потом рисовать по одному из JS — упрётесь в ту же Canvas 2D.

**GPU-instancing.** Один `drawArraysInstanced` на всю сцену. JS не трогает узлы: `Float32Array` — view на линейную память Wasm, один `bufferData`. 100k квадов для видеокарты — шум (0.03 ms CPU, FPS не падает). Камера — только uniform, буфер инстансов не пересобирается.

**Что ещё станет узким, если расти дальше (и что делает Figma):** нет отсечения вне кадра и тайлового кэша — 1M объектов pack+upload начнут стоить миллисекунды; нет текста (глифы — отдельная подсистема); нет мультиплеера (сервер-авторитет + мелкие патчи свойств). Список слоёв на 100k специально не виртуализирован: в UI только счётчик и выделение.

## Что дал Wasm, что дал GPU

- **Wasm** — владение документом: компактный SoA, hit-testing, жесты, undo/redo, бинарный `.mfg`. JS не держит копию дерева и не сериализует JSON на каждый кадр. Это модель и команды, как у Figma.
- **GPU** — почти весь выигрыш в FPS. Разница 0.03 ms vs 125 ms на 100k — это instancing против поэлементного Canvas 2D, не «Rust быстрее JS в цикле».
- Вместе: можно панорамировать 100k объектов без копии сцены в JS. По отдельности (Wasm+Canvas или JS+WebGL) картинка хуже.

## Вывод

Архитектура Figma на этом прототипе сходится к двум решениям: документ живёт в Wasm, пиксели рисует GPU пачками. Тонкий JS — панели и ввод. Наивная схема «массив объектов + Canvas 2D» умирает между 10k и 100k; связка Wasm+WebGL2 на 100k даже не заметна. Дальше, как у Figma, имеет смысл добавлять тайлы, отсечение и текст — не «ещё чуть оптимизировать JS».
