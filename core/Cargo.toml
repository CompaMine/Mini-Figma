[package]
name = "figcore"
version = "0.1.0"
edition = "2021"
description = "Wasm document core for a Figma-like research prototype"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
